# Frontend Checkpoint 1

**Дата:** 01.12.2025

## Что сделано

### Инфраструктура
- Vite + React + TypeScript
- Tailwind CSS v4
- shadcn/ui (button, card, input, badge)
- React Router
- React Query
- Zustand
- Axios (установлен, пока не используется)

### Структура проекта
```
frontend/src/
├── api/                  # API с моками
│   ├── mock-data.ts      # Тестовые данные
│   ├── products.ts       # getProducts, getProduct, getCategories
│   ├── recommendations.ts # getRecommendations, sendFeedback
│   ├── auth.ts           # login, register, getMe
│   ├── orders.ts         # getOrders, createOrder
│   └── client.ts         # Axios instance (для реального API)
│
├── components/
│   ├── ui/               # shadcn компоненты
│   ├── layout/           # Header, PageLayout
│   └── cart/             # AddToCartButton
│
├── hooks/                # React Query хуки
│   ├── useProducts.ts
│   ├── useRecommendations.ts
│   └── useOrders.ts
│
├── store/                # Zustand
│   ├── authStore.ts      # user, token, login, logout
│   └── cartStore.ts      # items, addItem, removeItem, updateQuantity
│
├── pages/                # Страницы
│   ├── HomePage.tsx      # Каталог с категориями
│   ├── ProductPage.tsx   # Карточка товара + рекомендации
│   ├── CartPage.tsx      # Корзина
│   ├── ProfilePage.tsx   # Личный кабинет
│   ├── OrderHistoryPage.tsx
│   ├── LoginPage.tsx
│   └── RegisterPage.tsx
│
└── types/                # TypeScript типы
    └── index.ts
```

### Функциональность

**Работает:**
- Каталог товаров с фильтрацией по категориям
- Страница товара с описанием
- Рекомендации с кнопками фидбека (👍/👎)
- Корзина: добавление, удаление, изменение количества
- Кнопка "В корзину" → счётчик [- N +]
- Оформление заказа → переход в историю
- Авторизация (логин/регистрация)
- Профиль пользователя
- История заказов
- Счётчик товаров в хедере
- Показ имени пользователя после авторизации
- Персистентность корзины и авторизации (localStorage)

**Моки:**
- 8 товаров (штукатурки, грунтовки, инструменты, электрика, гипсокартон)
- 5 категорий
- 2 тестовых заказа
- Фейковая авторизация (любой email, пароль > 3 символов)

## Что осталось по фронтенду

### Этап 2: Доработка каталога
- [ ] Компонент ProductCard (вынести из HomePage)
- [ ] Компонент ProductGrid
- [ ] Пагинация
- [ ] Фильтр по цене
- [ ] Поиск по названию

### Этап 3: Рекомендации
- [ ] Карусель рекомендаций на главной
- [ ] Рекомендации в корзине
- [ ] Компонент RecommendationCard (вынести)

### Этап 4: Корзина
- [ ] Рекомендации в корзине (блок "Добавьте к заказу")

### Этап 5: Авторизация
- [ ] ProtectedRoute компонент
- [ ] Редирект после логина на предыдущую страницу

### Этап 6: Polish
- [ ] Адаптивная вёрстка (мобильная версия)
- [ ] Скелетоны загрузки вместо "Загрузка..."
- [ ] Toast уведомления (добавлено в корзину, заказ оформлен)
- [ ] Пустые состояния с иллюстрациями
- [ ] 404 страница
- [ ] Улучшение дизайна

### Интеграция с бэкендом
- [ ] Переключить API с моков на реальные запросы
- [ ] Обработка ошибок API
- [ ] Рефреш токена

## Запуск

```bash
cd frontend
npm run dev
```

Откроется на http://localhost:5173

## Тестовые данные для входа

- Email: любой
- Пароль: любой (минимум 3 символа)
