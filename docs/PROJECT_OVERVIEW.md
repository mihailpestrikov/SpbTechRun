# Система персонализированных рекомендаций для онлайн-магазина

## Кейс

Интеллектуальная система подбора сопутствующих товаров для этапа ремонта White Box (предчистовая отделка) с непрерывным обучением на основе действий пользователей.

**Пример:** пользователь смотрит штукатурку → система рекомендует шпатели, грунтовку, маяки.

---

## Архитектура

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Frontend   │────▶│   Backend    │────▶│  ML Service  │
│    React     │     │     Go       │     │    Python    │
└──────────────┘     └──────────────┘     └──────────────┘
                            │                    │
                            ▼                    ▼
                     ┌──────────────┐     ┌──────────────┐
                     │  PostgreSQL  │     │    Ollama    │
                     └──────────────┘     └──────────────┘
```

### Компоненты

| Компонент | Технологии                              | Ответственность |
|-----------|-----------------------------------------|-----------------|
| Frontend | React, MUI                              | Витрина магазина, UI для фидбека |
| Backend | Go, Gin/Fiber, ELACTIC SEARCH, grafana? | REST API, авторизация, бизнес-логика |
| ML Service | Python, FastAPI                         | Генерация рекомендаций, обработка фидбека |
| Database | PostgreSQL                              | Товары, пользователи, фидбек, статистика |
| LLM | Ollama (локально)                       | Генерация эмбеддингов товаров |

---

## API

### Рекомендации

```
GET /recommendations/{product_id}?user_id=123

Response:
{
  "recommendations": [
    {
      "product_id": 205,
      "name": "Шпатель фасадный 350мм",
      "score": 0.92,
      "reason": "Необходим для работы со штукатуркой"
    },
    ...
  ]
}
```

### Фидбек

```
POST /feedback

Body:
{
  "user_id": 123,
  "main_product_id": 101,
  "recommended_product_id": 205,
  "feedback": "positive"  // или "negative"
}
```

---

## Как работают рекомендации

### Холодный старт (нет данных)

1. **Эмбеддинги (Ollama)** — находим семантически близкие товары
2. **Правила категорий** — фильтруем, оставляя сопутствующие (не аналоги)
3. **Выдаём топ-20** отсортированных по семантической близости

### С накопленным фидбеком

К базовому алгоритму добавляются:

4. **Глобальная статистика** — пары товаров с высоким % одобрения поднимаются вверх
5. **Профиль пользователя** — учитываем его предпочтения по категориям, брендам, бюджету

---

## Обучение системы

**Важно:** Ollama не дообучается. "Обучение" — это накопление статистики.

| Что меняется | Как обновляется |
|--------------|-----------------|
| Счётчики пар товаров | При каждом фидбеке (+1 к positive или negative) |
| Профили пользователей | При каждом фидбеке (обновление предпочтений) |

Никакого переобучения нейросетей в реальном времени — только инкремент счётчиков в БД.

---

## Структура данных

### Основные таблицы

- **products** — каталог товаров с эмбеддингами
- **categories** — иерархия категорий
- **category_rules** — какие категории сочетаются (экспертные правила)
- **feedback** — сырые оценки пользователей
- **feedback_stats** — агрегированная статистика по парам товаров
- **user_profiles** — предпочтения пользователей (категории, бренды, бюджет)

---

## Этапы разработки

### 1. Подготовка данных
- Парсинг YML-фида
- Генерация эмбеддингов через Ollama
- Построение FAISS-индекса
- Составление правил категорий для White Box

### 2. MVP рекомендаций
- Холодный старт: эмбеддинги + правила
- API для получения рекомендаций
- Базовый фронтенд с отображением рекомендаций

### 3. Система фидбека
- UI для оценки (👍/👎)
- Сохранение фидбека в БД
- Влияние статистики на ранжирование

### 4. Персонализация
- Профили пользователей
- Учёт предпочтений при ранжировании

### 5. Визуализация
- Отображение причин рекомендации
- Графики/аналитика для презентации

---

## Требования из условий хакатона

| Требование | Как выполняем |
|------------|---------------|
| Локальная LLM (Ollama) | Эмбеддинги через Ollama |
| Работа в закрытом контуре | Всё локально, без внешних API |
| Время ответа < 5 сек | FAISS для быстрого поиска, кэширование |
| Непрерывное обучение | Обновление статистики при каждом фидбеке |
| Визуализация инсайтов | Причины рекомендаций, графики в UI |

---

## Команда

| Роль | Задачи |
|------|--------|
| Backend (Go) x2 | API, авторизация, интеграция с ML-сервисом |
| ML Engineer | Python-сервис, эмбеддинги, алгоритм рекомендаций |
| Frontend | React-приложение, UI фидбека |
| UI/UX | Дизайн витрины и блока рекомендаций |

---

## Файлы документации

- `docs/PROJECT_OVERVIEW.md` — этот файл, общее описание
- `docs/ML_RECOMMENDATION_SYSTEM.md` — детальное описание ML-части
